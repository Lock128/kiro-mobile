import 'package:flutter/material.dart';

/// Stub for native platforms. Never instantiated (guarded by kIsWeb).
class SignInViewWeb extends StatelessWidget {
  const SignInViewWeb({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnsupportedError('SignInViewWeb is not supported on this platform');
  }
}
